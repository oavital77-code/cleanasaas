# מדיניות אבטחה — Cleana (SaaS)

## דיווח על פגיעות

**אל תפתחו Issue ציבורי על פגיעת אבטחה.** Issue נקרא על ידי כולם, כולל מי
שיכול לנצל אותה, והמערכת מחזיקה נתוני מטופלים ותשלומים של קליניקות אמיתיות.

הדרך הנכונה: **Security → Report a vulnerability** בריפו הזה. זהו ערוץ
דיווח פרטי של GitHub — רק בעלי הריפו רואים אותו, והוא מנוהל כשיחה עד
שהתיקון עולה לאוויר.

בדיווח כדאי לכלול: מה הפגיעות, איך לשחזר אותה צעד־אחר־צעד, ומה ההשפעה
בפועל (איזה מידע נחשף / איזו פעולה מתאפשרת). קוד הוכחה עוזר מאוד.

נשתדל לאשר קבלה תוך שלושה ימי עסקים.

## מה בתחום ומה לא

**בתחום, ובראש סדר העדיפויות:** כל דליפה בין קליניקות. זו מערכת רב־דיירית
שבה כל הקליניקות חולקות מסד נתונים אחד ומופרדות ב-`clinic_id` ובמדיניות
RLS — ולכן כל נתיב שמחזיר שורה של קליניקה אחת למשתמש של אחרת הוא הממצא
החמור ביותר שיש כאן. אותו דבר לגבי הסלמת הרשאות (מטפל שמשיג יכולות אדמין,
אדמין שמשיג superadmin), עקיפת שכבת החיוב, ופגמים בזרימת PayPlus.

שווה תשומת לב מיוחדת: פונקציות `security definer` ב-`supabase/migrations/`.
הן רצות בהרשאות היוצר ועוקפות RLS מעצם הגדרתן, ולכן פונקציה כזו שחסרה בה
בדיקת `clinic_id` היא בדיוק החור שה-RLS אמור היה לסגור. גם ההרשאות עליהן:
ב-Postgres, `execute` ניתן ל-`public` כברירת מחדל, כך ש-`revoke ... from
anon` לבדו לא עושה כלום — הדפוס הנכון הוא `revoke all ... from public,
anon` ואז `grant execute` לתפקיד שצריך.

**לא בתחום:** דיווחי סורק אוטומטי בלי הוכחת ניצול, הנדסה חברתית, DoS
בנפח, ופגיעויות בשירותי הצד־השלישי עצמם — Clerk, Supabase, Resend, PayPlus,
Sentry ו-Vercel מפעילים תוכניות דיווח משלהם, ושם המקום לדווח עליהן.

**מחוץ לתחום לחלוטין:** `cleana.co.il` — המערכת החד־דיירית המקורית. היא
פרויקט אחר, בפרודקשן, ואינה קשורה לריפו הזה.

## סודות

בריפו הזה אין ולא היו מפתחות אמיתיים. כל הסודות יושבים כמשתני סביבה
ב-Vercel; `.env.example` מתעד אילו משתנים קיימים ומה תפקידם, וכל הערכים בו
ריקים או דוגמה מפורשת.

`SUPABASE_SERVICE_ROLE_KEY` הוא החמור שבהם: הוא עוקף RLS לחלוטין וקורא כל
שורה של כל קליניקה. הוא נקרא אך ורק מ-`lib/supabase/admin.ts`, אך ורק
בקוד שרת, ואסור שיגיע ל-bundle של הדפדפן בשום תנאי. כל משתנה עם התחילית
`NEXT_PUBLIC_` נחשף ללקוח מעצם הגדרתו — לכן הוא לעולם לא נושא סוד.

זה נאכף, לא רק מוצהר: `.github/workflows/secret-scan.yml` מריץ Gitleaks על
**כל היסטוריית ה-git** בכל דחיפה ובכל שבוע, כי מפתח שנמחק בקומיט מאוחר
עדיין קריא בקומיט שהוסיף אותו.

אם בכל זאת נחשף מפתח — קודם מבטלים ומחליפים אותו אצל הספק (ב-Supabase:
Settings → API → Rotate), ורק אחר כך מטפלים בהיסטוריה. מחיקה מההיסטוריה
בלי החלפה אצל הספק לא מגינה על כלום: הערך כבר הופץ.

## סריקות אוטומטיות

| סריקה | מה מוצאת | מתי רצה |
| --- | --- | --- |
| CodeQL | פגמי אבטחה בקוד (הזרקה, redirect לא בטוח, סוד קשיח) | דחיפה, PR, שבועי |
| Gitleaks | מפתחות בכל היסטוריית ה-git | דחיפה, PR, שבועי |
| Dependency review | תלות חדשה עם CVE ידוע — חוסמת את ה-PR | PR |
| npm audit | CVE בתלויות הקיימות | PR, שבועי |
| Dependabot | עדכוני גרסה ותיקוני אבטחה | שבועי |

סריקות הקוד לא מכסות את מדיניות ה-RLS עצמה — היא SQL, לא TypeScript.
לשם כך יש את `supabase/tests/` ואת Supabase Advisors (Database → Advisors),
ושתיהן צריכות לרוץ אחרי כל מיגרציה שנוגעת בהרשאות.
